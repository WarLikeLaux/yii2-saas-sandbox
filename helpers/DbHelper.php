<?php

declare(strict_types=1);

namespace app\helpers;

/**
 * Помощник для подготовки значений к SQL-запросам.
 *
 * Инкапсулирует повторяемую подготовку строк для запросов (сейчас — экранирование
 * спецсимволов `LIKE`), чтобы сервисы не держали у себя эти технические детали.
 */
class DbHelper
{
    /**
     * Экранирует спецсимволы `LIKE` (`%`, `_`, `\`) в пользовательском вводе.
     *
     * @param string $value Исходная подстрока
     * @return string Подстрока, безопасная для подстановки в шаблон `LIKE`
     */
    public function escapeLike(string $value): string
    {
        return strtr($value, ['\\' => '\\\\', '%' => '\\%', '_' => '\\_']);
    }
}
